[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run src/streamlit/app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 100
